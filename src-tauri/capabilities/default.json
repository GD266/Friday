{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Phase 1 capability set: core window only. No shell, filesystem, or process APIs are enabled.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
